# Pixelalty assets

All five supplied PNG assets are retained unchanged in `assets/brand/` as `logo.png`, `launch.png`, `growth.png`, `premium.png`, and `advanced.png`. The original upload spelling “Luanch” is normalized to “launch” in the output filename only.

WebP derivatives preserve aspect ratio and all visible package graphics, including tier labels. The header uses a tightly framed copy of the supplied wordmark, with CSS inversion on a dark background. The favicon uses the supplied pixel P; no substitute P design was invented. Full official graphics appear on the relevant package pages and the original logo on About.

`assets/og-image.png` is the requested 1200×630 social-sharing composition generated using the official logo as reference. Its image-generation prompt called for the original pixelated P and Pixelalty lettering, white on near-black, “Digital Solutions,” “Web design. Listing videos.”, restrained blue pixel accents, and minimal architectural rules. The built-in image generator was used; the output was resized to the required sharing-card dimensions. This card is separate from the unchanged source logos.

The homepage “Studio / Form” visual is a fictional CSS/HTML **Design Concept**, explicitly labeled. It is not a customer site, case study, or claimed portfolio client. The video placeholder contains no property footage or MLS imagery. No photographs, testimonials, or fake statistics were sourced or invented.

Manrope is self-hosted in `assets/fonts/` under its included SIL Open Font License. The bundled Supabase browser client and its license are in `js/vendor/`. The public site does not request Google Fonts or a JavaScript CDN at runtime.

`assets/listing-video-demo.mp4` is intentionally absent. Upload the actual authorized video to that path later; the page automatically detects it.
